<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Demian Hallyfax</title>
    <link rel="stylesheet" href="../public/assets/styles.css" type="text/css" />
    <link rel="icon" type="image/x-icon" href="../public/imgs/icon.ico" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <body>
    <%- include('./partials/nav') %>

    <div class="container">

<!-- ~~~~~ ABOUT ~~~~~ -->
      <div class="cont-about"><%- include('./partials/about') %></div>

<!-- ~~~~~ CONTENIDO DEL BLOG ~~~~~ -->
      <div class="cont-blog">
        <h1>Ultimas entradas</h1>
        <% for(let i = 0; i+1 <= 3; i++) {%>
        <div class="content">
          <div>
            <a href="/blog/<%= articles[i].slug %>"
              ><img src="<%= articles[i].imgTemp %>" alt=""
            /></a>
          </div>
          <div>
            <a href="/blog/<%= articles[i].slug %>"
              ><h2><%= articles[i].title %></h2></a
            >
            <p><%= articles[i].description %></p>
          </div>
        </div>
        <% } %>

<!-- ~~~~~HISTORIAS ~~~~~ -->
        <h1>Mis Historias</h1>
        <div class="cont-fanfic">
          <% for(let i = 0; i < 6; i++) {%>
          <div>
            <a href="/stories/<%= stories[i].idName %>?chapter=1"
              ><img src="<%= stories[i].img %>" alt=""
            /></a>
          </div>
          <% } %>
        </div>
      </div>
    </div>

    <%- include('./partials/footer') %>
  </body>
</html>
